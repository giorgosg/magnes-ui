node_modules/
elm-stuff/
public/main.js
.dev/

# Issue tracker working notes (see docs/agents/issue-tracker.md). The tickets are
# scratch state for agents, not a record the repo needs to carry.
.scratch/

# Local instance address; see public/config.example.js
public/config.js

# Working notes about a particular deployment — addresses, hostnames, paths.
# The docs in docs/ describe bitmagnet and Magnes; anything naming *your*
# instances belongs in a *.local.md beside them, and stays out of the repo.
*.local.md

# Playwright output.
/test-results/
/playwright-report/
/blob-report/
